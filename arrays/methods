arr.unshift():
-- Adds one or more elements to the beginning of an array and returns the new length.
arr.shift(): 
-- Removes the first element from an array and returns that removed element.

arr.slice()
--returns the copy of a section of
elements
--the last range is not included

arr.splice()
--the last range is included
--main thins is array is manipulated

for Ex:
arr= [1,2,3,4,5,6]

const arr1 = arr.slice(1,3)
console.log(arr1);

output--> [1,2,3]
but main arr is --> [1,2,3,4,5,6]

const arr2= arr.splice(1,3)
output -->[1,2,3]
the main arar now is --> [4,5,6]

push()
--will adds anyhting object,arr,element

concat()
--concated two arrays and returns a new one

if arr has arrays inside it
const arr1= [1,2,[3,4,5],6,7]

---use flat
    -- to spread out all the elements
const real_another_array = arr1.flat(Infinity)

//[1,2,3,4,5,6,7]


isArray()
--returns whether it was an array or not

.from()
--creates an array if possible from it
  ex : console.log(Array.from("Kousik"))
  --output : ['K','o','u','s','i','k']
  --if not possible to make 
    --it returns empty
    ex: Array.from({name:"kousik"})
    output : []


.of()
--creates an array from individual elements
  --Array.of(100,200,300)
  output : [100,200,300]
